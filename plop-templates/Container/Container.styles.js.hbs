import { ScaledSheet } from "react-native-size-matters";
import { Metrics, Helpers } from "{{backRootPathCase name}}themes";

export default (styles = ScaledSheet.create({
    ...Helpers,
    container: {
        width: "100%"
    },
}));